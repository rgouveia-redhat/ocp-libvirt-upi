#!/bin/bash

cd /root
rm -rf install-dir && mkdir install-dir
cp install-config.yaml install-dir
openshift-install --dir ./install-dir/ create ignition-configs

cp install-dir/*.ign /var/www/html/
chown apache:apache /var/www/html/*.ign
restorecon -F /var/www/html/
ls -l /var/www/html/*.ign
